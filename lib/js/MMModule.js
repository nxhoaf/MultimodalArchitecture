console.log("MMModule.js loaded"); 

// Variables used here are application-depended, and declared in MC_config.
<!-- the XHR API to handle the external async communication -->
document.write(MMDispatcherPath);